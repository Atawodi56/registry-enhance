[project]
name = "registry-enhance"
authors = ["Registry Enhance Team"]
description = "Enhanced asset registry and tokenization platform"
telemetry = false
requirements = []
[contracts.asset_registry]
path = "contracts/asset_registry.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
